version: '3.8'

services:
  postgres:
    image: postgres:16-alpine
    container_name: sqlix_postgres
    restart: always
    environment:
      POSTGRES_DB: sqlix_db
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: "11111111"
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: